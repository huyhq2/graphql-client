interface RelevantBookListProps {
  authorId: string;
}
//TODO: query data
const RelevantBookList = ({ authorId }: RelevantBookListProps) => {
  console.log(authorId);

  return <div>Todo: RelevantBookList</div>;
};

export default RelevantBookList;
